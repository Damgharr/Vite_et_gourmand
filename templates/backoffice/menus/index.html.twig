{% extends 'backoffice_base.html.twig' %}
{% block title %}Menus{% endblock %}
{% block backoffice_body %}
    <h1>Menus</h1>
    <a href="{{ path('employee_menus_new') }}" class="cta">Nouveau menu</a>
    <ul>
        {% for menu in menus %}
            <li>
                {{ menu.title }} — {{ menu.pricePerPeople }}€/pers (min {{ menu.minPeopleAmount }})
                <a href="{{ path('employee_menus_edit', {id: menu.id}) }}">Modifier</a>
                <form method="post" action="{{ path('employee_menus_delete', {id: menu.id}) }}">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.id) }}">
                    <button>Supprimer</button>
                </form>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
